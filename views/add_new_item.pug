extends layout

block content
  h2 #{title}
  form(method='POST', action='/articles/add_new_item')
    .form-group
      label List_item:
      input.form-control(name='list_item', type='text')
    br
    input.btn.btn-outline-dark(type='submit', value='Hinzufügen')